# Repo Health Checker

This project checks repository health using GitHub Actions.

Checks included:
- README existence
- README line count
- .gitignore existence
- .env file detection

The checks run automatically on every push.
